# 借鉴：青春恋爱物语模拟器 - 数值系统与游戏机制

> 来源：Moshenxy/youth-romcom-simulator
> 提取日期：2026-06-12

---

## 一、核心数值系统（值得完美借鉴）

### 1. **三维关系系统**（vs 你的单一 affinity）

```typescript
关系 {
  亲密度: {
    value: number          // 数值
    saturation: 0-100      // 饱和度（到100后需要突破事件才能继续增长）
    bottleneck: boolean    // 是否遇到瓶颈
    stage: string          // 阶段名称
    recentInteractions: [] // 最近互动记录
  }
  支配度: { ... }          // 控制关系（rebellion → puppet）
  信赖度: { ... }          // 信任程度
}
```

**为什么比你的好：**
- 你的项目：`affinity` 单一数值，线性增长
- 对方：三维独立，亲密度高不等于信赖度高（可以做"亲密但不信任"的关系）
- **饱和度机制**：防止无脑刷好感，必须有剧情事件才能突破瓶颈

**借鉴建议：**
```typescript
// 你的 attributes 表可以扩展：
{
  targetId: 'megumi',
  key: 'intimacy',           // 亲密度
  value: 75,
  metadata: {
    saturation: 90,          // 新增：饱和度
    bottleneck: true,        // 新增：瓶颈标记
    stage: '恋人未满',       // 新增：阶段名
  }
}
```

---

### 2. **六维属性系统**（vs 你没有玩家属性）

```typescript
属性 {
  沟通: { rank: 'B', modifier: +5, experience: 1200 }
  观察: { rank: 'A', modifier: +8, experience: 2100 }
  行动力: { rank: 'C', modifier: +2, experience: 800 }
  精神力: { rank: 'S', modifier: +12, experience: 3500 }
  知识: { rank: 'A', modifier: +8, experience: 1900 }
  魅力: { rank: 'B', modifier: +5, experience: 1400 }
}

// Rank: F → E → D → C → B → A → S → SS → SSS
// modifier: -5 (F) 到 +20 (SSS)
```

**为什么重要：**
- 你的项目：User 是"无属性玩家"，只有关系数值
- 对方：User 有**成长系统**，玩家能感受到"我变强了"
- 技能检定可以用 DC（难度等级）对抗属性

**游戏性提升：**
```
场景：英梨梨画废了一张稿子，正在崩溃边缘
选项：
1. 【沟通 B+】"你已经很努力了，休息一下吧"
2. 【观察 A+】注意到她握笔的手在发抖，递上热巧克力
3. 【行动力 C】直接帮她重画（失败：你画得更烂，她更崩溃）
```

**借鉴建议：**
- 在 `statusData` 加 `userAttributes` 字段
- 快捷回复选项显示属性要求：`>【沟通B+】温柔安慰她`
- DC检定失败时触发不同剧情分支

---

### 3. **技能系统**（你完全没有）

```typescript
技能 {
  name: '读心术',
  type: 'passive',           // active / passive
  effectType: 'numeric',     // numeric / text
  numericEffect: {
    attribute: '观察',
    operation: 'add',        // add/subtract/multiply/divide
    value: 3
  },
  probability: 0.8,          // 触发概率
  description: '观察力+3，80%概率察觉他人情绪'
}
```

**游戏性：**
- 玩家可以"学技能"（读书、训练、被教导）
- 被动技能改变属性检定
- 主动技能消耗行动点

**借鉴建议：**
- 在 MemoryDB 加 `skills` 表
- 英梨梨可以教你画画（习得技能：`绘画入门 → 观察+2`）
- 诗羽可以教你写作（习得技能：`文笔流畅 → 沟通+3`）

---

### 4. **行动点系统**（你没有）

```typescript
worldState {
  actionPoints: number  // 每天有限行动点
}
```

**为什么重要：**
- 你的项目：可以无限对话，没有"一天结束"的节奏感
- 对方：每天 X 点行动力，用完就到晚上，强制推进时间

**游戏性：**
```
早上：2点行动力
- 去学校（消耗1点） vs 翘课约英梨梨（消耗2点）
中午：1点行动力  
- 食堂吃饭（0点） vs 天台找诗羽（1点）
放学：3点行动力
- 社团活动（1点） vs 陪加藤买菜（2点） vs 去秋叶原（3点）
```

**借鉴建议：**
- 在 `statusData.world` 加 `actionPoints` 和 `maxActionPoints`
- 快捷选项标注消耗：`>【2AP】陪英梨梨去画材店`
- 副API分析时扣除行动点

---

### 5. **NPC内心世界系统**（你的印象系统可以扩展）

```typescript
NPC内心 {
  mentalHealth: 0-100,      // 精神状态
  状态: 'collapsed' | 'chaotic' | 'pressured' | 'stable' | 'elevated',
  
  核心动机: {
    生存: 30,               // 五个维度的动机强度
    快乐: 70,
    权力: 20,
    关系: 85,
    意义: 60
  },
  
  创伤恐惧: [
    { trigger: '被否定创作', stressResponse: '逃避+暴食' }
  ],
  
  NSFW进度: {
    phys: 0-6,              // 身体接受度（0=牵手都脸红 → 6=全垒打）
    mind: 0-5               // 心理接受度
  },
  
  记忆: [
    { timestamp, content, emotionalTag, impactNote }
  ]
}
```

**为什么重要：**
- 你的项目：`impressions` 只是简单的 `polarity/weight/reason`
- 对方：**动机驱动行为** + **创伤系统** + **分离的身心NSFW进度**

**游戏性差异：**
```
你的项目：
impression: { label: '守信', polarity: 1, weight: +2 }

对方项目：
mentalHealth: 35 (chaotic)
核心动机.关系: 85 → 英梨梨极度渴望认可
创伤: '被伦也否定' → 触发时 stress += 20
NSFW: phys=2 (接受拥抱), mind=1 (心理上还不敢越界)
```

**借鉴建议：**
- `impressions` 表加字段：`mentalHealthImpact`, `motivationAffected`
- 新增 `traumas` 表（创伤触发器 + 应激反应）
- 性进度拆成 `physicalIntimacy` 和 `mentalIntimacy` 两个独立轴

---

### 6. **人格内核 - 行为逻辑**（你的审计规则可以结构化）

```typescript
人格内核 {
  behavioralLogic: {
    publicMode: '在校内维持大小姐人设',
    privateMode: '在User面前可以卸下伪装'
  },
  
  behavioralProfiles: [
    {
      situation: '创作被否定',
      response: '先暴怒反驳，然后独自躲起来自我怀疑',
      motivation: '用愤怒掩盖脆弱'
    }
  ],
  
  dialogueStyle: {
    catchphrases: ['八嘎伦也！', '才、才不是因为...'],
    tabooWords: ['承认喜欢', '示弱']
  }
}
```

**vs 你的审计规则：**
- 你的：写在 `relationship.ts` 的长文本提示词里
- 对方：**结构化数据**，可以动态查询和组合

**借鉴建议：**
- 把审计规则从长文本改成 JSON 配置
- 存在世界书或单独的 `personality-profiles.json`
- prompt 构建时根据当前 `situation` 动态加载对应 profile

---

## 二、游戏循环机制

### 7. **日程表系统**（你没有）

```typescript
schedule {
  forcedSchedules: [
    {
      time: '周一 08:00-12:00',
      event: '上课',
      location: '总武高中-2年F班',
      interruptCheck: { skill: '行动力', DC: 15 }  // 翘课需要过检定
    }
  ],
  
  dailyTendencies: {
    morning: { location: '便利店', probability: 0.6 },
    noon: { location: '天台', probability: 0.8 }
  }
}
```

**游戏性：**
- 强制日程：上学、社团、考试（想翘课要过DC检定）
- 日常倾向：英梨梨早上60%概率去便利店（玩家可以"偶遇"）

**借鉴建议：**
- 在 MemoryDB 加 `schedules` 表
- 每个女主有固定作息（增加"寻找她"的游戏性）
- User 可以选择"等在她常去的地方"

---

### 8. **卡牌系统**（你的剧情库可以升级）

```typescript
卡牌类型：
- 任务卡：触发支线任务
- 角色卡：新角色登场
- 物品卡：获得关键物品
- 事件卡：随机事件
- 命运卡：运势预测（每日抽一张）
- 属性卡：临时属性加成
- 技能卡：学习新技能
- 灾难卡：负面事件
- 纪念卡：里程碑记录
```

**vs 你的剧情库：**
- 你的：线性事件链（`currentMainEventId` 推进）
- 对方：**卡牌池抽取** + **多线并行**

**游戏性：**
```
每天早上抽一张命运卡：
- 「邂逅」：今天在图书馆遇到诗羽的概率+50%
- 「灵感」：创作类技能检定+3
- 「倒霉」：行动力消耗+1
```

**借鉴建议：**
- `plotLibrary` 改成 `cardPool`
- 每天随机激活 1-2 张卡
- 卡牌有权重和触发条件

---

### 9. **场景特质系统**（你的场景系统可以升级）

```typescript
location {
  traits: [
    {
      name: '樱花飘落',
      trigger: { month: 4, weather: '晴' },
      effect: '浪漫氛围，亲密度增长+20%'
    },
    {
      name: '暴雨',
      trigger: { weather: '大雨' },
      effect: '被困，强制二人独处事件'
    }
  ]
}
```

**游戏性：**
- 地点不是静态背景，而是有**动态特质**
- 天气、时间、在场人物影响场景特质

**借鉴建议：**
- 在 `statusData.world` 加 `activeLocationTraits`
- 美术教室晚上+无人 → 触发特质「秘密空间」 → NSFW概率+30%

---

## 三、你的项目缺失的核心玩法

### 10. **委托/任务系统**

```typescript
commission {
  id: string,
  title: '帮英梨梨完成原画',
  issuer: 'eriri',
  deadline: '2025-05-15',
  rewards: { affinity: +10, skill: '绘画入门' },
  status: 'active' | 'completed' | 'failed' | 'expired'
}
```

**为什么重要：**
- 你的项目：`tasks` 表只记录承诺，没有**奖励机制**
- 对方：完成任务 → 获得技能/物品/关系提升

---

### 11. **物品系统**（你的 items 表未激活）

```typescript
item {
  category: '服装',
  slot: 'coat',              // 服装有装备槽位
  seasonalTags: ['春', '秋'],
  effects: [
    { attribute: '魅力', value: +2 }
  ]
}
```

**游戏性：**
- 买新衣服 → 魅力+2 → 约会时触发特殊对话
- 送礼物 → 亲密度+X（但要送对口味，送错扣分）

---

### 12. **天气与预报系统**

```typescript
weather {
  current: '晴',
  forecast: ['晴', '多云', '雨']  // 未来三天预报
}
```

**游戏性：**
- 看天气预报 → 明天下雨 → 今天约英梨梨去水族馆（室内约会）
- 突然下雨 → 共撑一把伞事件

---

## 四、对方的"太重"问题

### 缺点分析：

1. **10w token 开局 prompt**
   - 把所有系统规则都塞进一个巨型世界书
   - 每次 API 调用都要传这么多上下文

2. **双 API 调用成本**
   - 主 API + 分析 API = 每轮双倍开销
   - 延迟高（要等两次生成）

3. **过度结构化**
   - `NPC内心世界` 包含太多字段（trauma、motivation、memory）
   - 实际游玩时可能90%的字段用不上

4. **卡牌系统复杂度**
   - 9种卡牌类型 × 多张卡 = 状态爆炸
   - 玩家可能记不住这么多机制

---

## 五、推荐借鉴的优先级

### P0（立即可借鉴，轻量改动）

1. ✅ **三维关系系统**（亲密度/支配度/信赖度 + 饱和度机制）
2. ✅ **NSFW分离进度**（phys/mind 独立轴）
3. ✅ **人格内核结构化**（把审计规则改成 JSON）
4. ✅ **场景特质**（天气/时间触发不同氛围）

### P1（需要新增子系统）

5. ⚠️ **玩家六维属性**（需要新增 `userAttributes` + DC检定逻辑）
6. ⚠️ **行动点系统**（需要改 `worldState` + 时间推进逻辑）
7. ⚠️ **日程表系统**（需要新增 `schedules` + 偶遇机制）
8. ⚠️ **技能系统**（需要新增 `skills` 表 + 学习/触发逻辑）

### P2（可选，增加深度但也增加复杂度）

9. 🔵 **卡牌系统**（剧情库升级为卡牌池）
10. 🔵 **委托任务系统**（`tasks` 表加奖励机制）
11. 🔵 **物品装备系统**（激活 `items` 表 + 装备槽位）
12. 🔵 **天气系统**（新增 `weather` + 预报）

### P3（不推荐，太重）

- ❌ 完整的 `mentalHealth` + `核心动机` 五维度（太复杂，AI 难以稳定输出）
- ❌ 创伤触发器系统（需要大量测试和调教）
- ❌ 双 API 架构（成本和延迟太高）

---

## 六、具体实施建议

### 立即可做（今天就能改）

#### 1. 三维关系系统

```typescript
// memorydatabase/types.ts
export type RelationshipDimension = 'intimacy' | 'dominance' | 'trust';

export type MemoryAttribute = MemoryBaseRow & {
  targetId: string;
  key: string;
  value: number;
  metadata?: {
    saturation?: number;      // 0-100，饱和度
    bottleneck?: boolean;     // 是否遇到瓶颈
    stage?: string;           // 阶段名称
    dimensionType?: RelationshipDimension;  // 关系维度类型
  };
};
```

#### 2. NSFW 分离进度

```typescript
// 现有的 sex_status 扩展
sexStatus: {
  virginityLatch: boolean,
  bodyCounters: { ... },
  physicalIntimacy: 0-6,    // 新增：身体接受度
  mentalIntimacy: 0-5,      // 新增：心理接受度
}
```

#### 3. 场景特质

```typescript
// statusData.world 扩展
world: {
  currentLocation: string,
  weather: '晴' | '阴' | '雨' | '雪',
  activeTraits: [
    { name: '樱花飘落', effect: 'intimacy_growth+20%' }
  ]
}
```

---

## 七、避免踩坑

### 不要做的事：

1. ❌ **不要全盘照搬** - 对方是通用框架，你的是 IP 项目，保留你的原著底盘优势
2. ❌ **不要做 10w token prompt** - 把规则分散到代码逻辑里，不要全塞世界书
3. ❌ **不要做双 API** - 保持你的单主 API + 轻量副 API 架构
4. ❌ **不要加太多数值** - 每加一个数值 = 玩家要记住 + AI 要计算 + 出 bug 概率+10%

### 要做的事：

1. ✅ **选择性借鉴** - 只拿对游戏性有明显提升的机制
2. ✅ **渐进式增加** - 先做 P0，测试稳定后再做 P1
3. ✅ **保持轻量** - 每个新系统都问自己：这能给玩家带来什么新体验？
4. ✅ **利用你的优势** - 你有原著 IP、审计规则、快捷选项，这些是对方没有的

---

## 八、总结

### 对方项目的精华（值得学习）：

1. **多维关系** - 解决你的单一 affinity 问题
2. **饱和度瓶颈** - 防止无脑刷好感
3. **玩家属性成长** - 给玩家"变强"的体感
4. **行动点限制** - 增加策略性和时间节奏感
5. **结构化人格** - 审计规则可维护性++

### 你的项目的优势（要保持）：

1. ✅ 原著 IP 底盘（对方是通用框架）
2. ✅ 快捷回复选项（对方没有）
3. ✅ 记忆库可视化（对方没有玩家端编辑器）
4. ✅ 单 API 架构（更轻更快）
5. ✅ 完整的手机系统（对方只有配置面板）

### 最佳融合策略：

**你的 IP 叙事 + 对方的数值系统 = 完美青春恋爱游戏**

- 保留你的：原著事件、审计规则、快捷选项、手机、记忆库
- 借鉴对方的：三维关系、饱和度、玩家属性、行动点、场景特质

**预期效果：**
- 玩家不再是"无限刷对话的幽灵"，而是"有属性、有限制、要做选择的角色"
- 关系不再是"线性刷好感"，而是"多维度、有瓶颈、需要突破事件"
- 游戏不再是"纯聊天"，而是"有节奏、有策略、有成长"的 Galgame

---

**需要我帮你实现哪个系统？建议从 P0 的三维关系系统开始。**
