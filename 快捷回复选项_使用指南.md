# 快捷回复选项功能 - 使用指南

## 功能概述

快捷回复选项功能为玩家提供四个符合角色人设的行动选项，以千恋万花风格的视觉小说选项卡片呈现。点击选项后自动填充到输入框并发送。

## 快速启用

### 方法 1：导入世界书条目（推荐）

1. 打开 `快捷回复选项_世界书条目示例.json`
2. 复制 `worldbook_entry` 对象的内容
3. 在 SillyTavern 中打开角色卡的世界书编辑器
4. 创建新条目并粘贴内容（或使用导入功能）
5. 保存角色卡

### 方法 2：手动添加提示词

在角色卡的系统提示词或世界书中添加：

```
在每次回复的 </content> 之后，生成四个快捷回复选项：

<options>
>选项一：[谨慎观察型选项]
>选项二：[积极推进型选项]
>选项三：[人际互动型选项]
>选项四：[情色/挑逗型选项]
</options>

每个选项 20-40 字，符合 user 人设和当前场景。
```

## 选项类型说明

| 类型 | 特点 | 示例 |
|------|------|------|
| 选项一 | 谨慎、观察、被动 | "保持沉默，观察她的反应" |
| 选项二 | 积极、主动、推进 | "直接问道：'你到底想说什么？'" |
| 选项三 | 关心、互动、情感 | "温柔地说：'我会一直陪着你的'" |
| 选项四 | 亲密、挑逗、NSFW | "轻轻握住她的手，凝视她的眼睛" |

## 视觉效果

选项以 2×2 网格布局显示：
- 淡紫色渐变背景
- 圆角卡片设计
- 悬停时轻微上浮
- 左侧箭头装饰（▸）
- 符合千恋万花美学

## 技术实现

### 前端组件

- **解析**：`extractOptionsBlock()` - 从 `<options>` 标签提取选项
- **渲染**：`renderOptionsPanel()` - 生成选项按钮面板
- **样式**：`.reader-options` - 视觉小说风格样式
- **交互**：点击选项 → 填充输入框 → 自动发送

### 标签格式

```xml
<content>
  正文内容...
</content>

<options>
>选项一：具体行动描述
>选项二：具体行动描述
>选项三：具体行动描述
>选项四：具体行动描述
</options>
```

## 常见问题

### Q: 选项没有显示？

A: 检查以下几点：
1. 世界书条目是否已启用
2. AI 回复中是否包含 `<options>` 标签
3. 选项格式是否正确（每行以 `>` 开头）
4. 消息是否已完全生成（非流式状态）

### Q: 如何调整选项风格？

A: 在世界书条目中修改选项类型说明，例如：
- 将选项四改为"幽默调侃型"以降低 NSFW 程度
- 添加更多场景示例来引导 AI 生成风格

### Q: 可以生成少于 4 个选项吗？

A: 可以，前端会自动适配任意数量的选项，但建议保持 4 个以获得最佳视觉效果。

### Q: 如何禁用此功能？

A: 在世界书中禁用相关条目，或从提示词中移除 `<options>` 生成指令。

## 自定义样式

如需修改选项卡片样式，编辑 `styles.css` 中的 `.reader-options` 相关样式：

```css
.reader-options__item {
  /* 修改背景色 */
  background: linear-gradient(...);
  
  /* 修改边框 */
  border: 2px solid ...;
  
  /* 修改字体 */
  font-size: 0.9rem;
}
```

## 示例场景

### 日常对话
```
<options>
>选项一：保持沉默，观察她的反应
>选项二：直接问道："你到底想说什么？"
>选项三：温柔地说："我会一直陪着你的"
>选项四：轻轻握住她的手，凝视她的眼睛
</options>
```

### 紧张冲突
```
<options>
>选项一：后退一步，评估当前局势
>选项二：毫不犹豫地冲上前去
>选项三：试图用言语缓和气氛
>选项四：将她护在身后，用身体挡住危险
</options>
```

### 亲密时刻
```
<options>
>选项一：移开视线，假装什么都没看到
>选项二：打破沉默："我们该谈谈了"
>选项三：温柔地抚摸她的脸颊
>选项四：将她拉入怀中，吻住她的唇
</options>
```

## 更多信息

详细技术文档请参阅 `QUICK_REPLY_OPTIONS.md`
