# 快捷回复选项 - 简化提示词（适配易掉格式的模型）

## 针对 DeepSeek V4 PRO / Gemini 3.0 Pro 等模型

这些模型容易掉格式，需要极简的指令。

---

## 最简版本（推荐）

```
每次回复在 </content> 后生成四个选项：

<options>
>选项一：观察等待
>选项二：主动行动
>选项三：关心互动
>选项四：亲密接触
</options>

每个选项 15-35 字，符合 user 性格和场景。
```

---

## 标准简化版

```
在 </content> 后必须生成选项：

<options>
>选项一：[谨慎观察类]
>选项二：[积极行动类]
>选项三：[情感互动类]
>选项四：[亲密挑逗类]
</options>

要求：
- 每个 15-35 字
- 符合 user 人设
- 第一人称行动
```

---

## 带示例版（中等长度）

```
在 </content> 后生成四个选项：

<options>
>选项一：谨慎观察型，如"保持沉默，观察反应"
>选项二：积极推进型，如"直接问：'怎么了？'"
>选项三：情感互动型，如"轻声安慰她"
>选项四：亲密挑逗型，如"握住她的手"
</options>

每个选项 15-35 字，符合当前场景。
```

---

## 世界书条目简化版

```json
{
  "uid": 9999,
  "key": ["选项", "options"],
  "comment": "快捷回复",
  "content": "在 </content> 后生成：\n\n<options>\n>选项一：观察等待\n>选项二：主动行动\n>选项三：关心互动\n>选项四：亲密接触\n</options>\n\n每个15-35字，符合user性格。",
  "constant": true,
  "order": 100,
  "position": "after_char",
  "enabled": true
}
```

---

## 格式保持技巧

针对容易掉格式的模型：

1. **极简指令**：只说必须的
2. **明确标签**：强调 `<options>` 和 `>` 符号
3. **示例引导**：给一两个短例子
4. **固定位置**：明确在 `</content>` 后
5. **字数限制**：15-35字，防止过长导致格式崩溃

---

## 推荐配置

### DeepSeek V4 PRO
使用**最简版本**，放在 system prompt 或高优先级世界书条目。

### Gemini 3.0 Pro  
使用**标准简化版**，可以稍微详细一点但不要超过 5 行。

### 其他稳定模型
可以使用原完整版提示词（`快捷回复选项_世界书条目示例.json`）。

---

## 测试方法

1. 添加简化提示词
2. 发送一条测试消息
3. 检查 AI 回复是否包含格式正确的 `<options>` 标签
4. 如果掉格式，进一步简化或调整位置

---

## 常见掉格式问题

### 问题 1：忘记生成选项
**解决**：在提示词开头加"必须"或"每次都要"

### 问题 2：格式错乱（忘记 `>` 或标签）
**解决**：示例中明确展示完整格式

### 问题 3：选项过长导致换行混乱
**解决**：明确"15-35字"限制

### 问题 4：选项放在错误位置
**解决**：强调"在 </content> 后"

---

## 对比：完整版 vs 简化版

| 特性 | 完整版 | 简化版 |
|------|--------|--------|
| 字数 | ~800字 | ~50字 |
| 详细度 | 高（含原则、示例、注意事项） | 低（只说必须的） |
| 适用模型 | GPT-4, Claude, 高质量模型 | DeepSeek, Gemini, 易掉格式模型 |
| 格式稳定性 | 高 | 中（需测试） |
| 灵活性 | 高（AI 理解更深） | 低（严格按格式） |

---

## 使用建议

1. **先试简化版**：从最简版本开始
2. **观察几轮**：看是否稳定生成
3. **逐步调整**：如果稳定，可以稍微加点细节；如果掉格式，继续简化
4. **记录有效配置**：找到适合该模型的最佳版本后保存

---

文件：`快捷回复选项_简化提示词.md`
