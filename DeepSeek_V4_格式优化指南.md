# DeepSeek V4 格式稳定性优化指南

## 问题背景

DeepSeek V4（包括 V4-Pro）会偶发掉格式问题：
- 段落结构混乱
- 漏标签（如忘记 `</content>` 或 `<options>`）
- 未按要求输出特定格式
- 输出冗余内容或多余解释

本指南提供立竿见影的解决方案。

---

## 1. 强化系统提示词

### ✅ 推荐写法

```
## 输出格式要求（严格遵守）

每次回复必须严格按以下结构输出，不要包含任何多余的对话或解释：

<content>
[正文内容]
</content>

<options>
>选项一：[15-35字]
>选项二：[15-35字]
>选项三：[15-35字]
>选项四：[15-35字]
</options>

格式要求：
- 必须包含 <content> 和 <options> 标签
- 选项必须以 > 开头
- 每个选项控制在 15-35 字
- 不要输出多余的解释或对话
```

### ❌ 避免的写法

```
请尽量按照以下格式输出... （太模糊）
你可以参考这个格式... （不够强制）
建议使用 <options> 标签... （容易被忽略）
```

---

## 2. Few-Shot 范例提示

在 system prompt 或高优先级世界书条目中加入 1-2 个完整范例：

```
## 输出格式范例

### 范例 1：日常对话

<content>
加藤惠平静地看着我，眼神中闪过一丝难以察觉的担忧。她轻声说："伦也君，你最近似乎很累呢。"
</content>

<options>
>选项一：保持沉默，观察她的反应
>选项二：直接问："你是不是有话要说？"
>选项三：苦笑着说："被你看出来了啊"
>选项四：轻轻握住她的手，凝视她的眼睛
</options>

### 范例 2：紧张场景

<content>
英梨梨猛地转过身，脸颊绯红，声音带着颤抖："你、你到底想怎样啊！"
</content>

<options>
>选项一：后退一步，给她空间冷静
>选项二：上前一步："我想知道答案"
>选项三：温柔地说："我只是担心你"
>选项四：将她逼到墙边，低声问："你害怕吗？"
</options>

---

请严格按照以上格式输出，每次回复都必须包含 <content> 和 <options> 两个部分。
```

**效果**：模型看过标准范例后，结构崩坏的概率会大幅降低。

---

## 3. 设定 Stop Words（停止词）

### SillyTavern 设置

在 SillyTavern 的 API 设置中添加停止序列：

```
停止序列：
- </options>
- ```
- ---

（根据实际情况调整）
```

### 作用

- 防止模型在 `</options>` 后继续输出冗余内容
- 避免模型自动补充代码块或分割线
- 让输出在正确位置干净截断

### 示例

**未设置停止词**：
```
<options>
>选项一：保持沉默
>选项二：主动询问
>选项三：安慰她
>选项四：握住她的手
</options>

（注：以上是根据当前场景生成的选项，您可以根据需要调整...）← 冗余输出
```

**设置停止词后**：
```
<options>
>选项一：保持沉默
>选项二：主动询问
>选项三：安慰她
>选项四：握住她的手
</options>
← 干净截断
```

---

## 4. API 参数优化

### Temperature 调降

```json
{
  "temperature": 0.3,
  "top_p": 0.9,
  "max_tokens": 2048
}
```

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| `temperature` | 0.2 - 0.4 | 降低随机性，提高格式稳定性 |
| `top_p` | 0.85 - 0.95 | 平衡创意与一致性 |
| `max_tokens` | 1500 - 2500 | 避免过长输出导致尾部掉格式 |

### 效果对比

| Temperature | 格式稳定性 | 创意程度 | 适用场景 |
|-------------|------------|----------|----------|
| 0.8 - 1.0 | ⭐⭐ | ⭐⭐⭐⭐⭐ | 创意写作（但易掉格式） |
| 0.4 - 0.7 | ⭐⭐⭐⭐ | ⭐⭐⭐ | 平衡（默认） |
| 0.2 - 0.4 | ⭐⭐⭐⭐⭐ | ⭐⭐ | 格式严格场景（推荐） |

---

## 5. 避免超长上下文

### 问题

当对话历史超过数万字时，模型在长文本尾端容易"注意力分散"，导致：
- 忘记格式要求
- 漏标签
- 输出结构混乱

### 解决方案

#### 方案 1：使用摘要系统

项目中已有 `summary/` 摘要系统，建议：
- 每 10-15 轮对话触发一次小总结
- 保留最近 5-8 条原始消息
- 将旧消息压缩成摘要

#### 方案 2：定期清理历史

在 SillyTavern 中：
- 使用"保存当前分支"功能保存进度
- 定期清理旧消息（保留关键转折点）
- 从保存点继续对话

#### 方案 3：分段处理长输出

如果需要生成长文本：
```
不要：一次性要求生成 5000 字剧情 + 选项
应该：先生成剧情，确认后再追加"现在生成选项"
```

---

## 6. 模型选择建议

### DeepSeek V4 系列对比

| 模型 | 格式稳定性 | 速度 | 成本 | 推荐度 |
|------|------------|------|------|--------|
| DeepSeek-V4-Pro | ⭐⭐⭐⭐ | 慢 | 高 | ⭐⭐⭐⭐⭐ |
| DeepSeek-V4 | ⭐⭐⭐ | 中 | 中 | ⭐⭐⭐⭐ |
| DeepSeek-V4-Flash | ⭐⭐ | 快 | 低 | ⭐⭐ |

**建议**：使用 **DeepSeek-V4-Pro** 以获得最佳格式遵从能力。

---

## 7. 实战配置模板

### 完整 System Prompt（针对 DeepSeek V4）

```
你是一个严格遵守输出格式的 AI 助手。

## 输出格式（必须严格遵守）

每次回复必须包含以下两个部分，不要有任何遗漏或多余内容：

<content>
[在这里写正文内容，描述场景、对话和动作]
</content>

<options>
>选项一：[15-35字的谨慎观察型行动]
>选项二：[15-35字的积极推进型行动]
>选项三：[15-35字的情感互动型行动]
>选项四：[15-35字的亲密挑逗型行动]
</options>

## 格式检查清单

在输出前，请确认：
- ✓ 包含 <content> 标签
- ✓ 包含 <options> 标签
- ✓ 每个选项以 > 开头
- ✓ 每个选项 15-35 字
- ✓ 没有多余的解释或对话

## 范例

<content>
加藤惠平静地看着我："伦也君，你最近很累吧。"
</content>

<options>
>选项一：保持沉默，观察她的反应
>选项二：直接问："你有话要说？"
>选项三：苦笑："被你看出来了"
>选项四：握住她的手，凝视她的眼睛
</options>

---

请严格按照以上格式输出，不要遗漏任何标签。
```

### API 参数设置

```json
{
  "model": "deepseek-chat",
  "temperature": 0.3,
  "top_p": 0.9,
  "max_tokens": 2000,
  "stop": ["</options>", "```", "---"]
}
```

---

## 8. 故障排查

### 问题 1：仍然漏标签

**可能原因**：提示词优先级太低，被其他世界书条目冲掉了

**解决方案**：
- 将格式要求放在 system prompt
- 或设置世界书条目 `order: 100`，`position: "after_char"`
- 启用 `constant: true` 确保每次都加载

### 问题 2：选项格式错乱

**可能原因**：Few-Shot 范例不够清晰

**解决方案**：
- 增加 2-3 个范例
- 在范例中明确标注每个部分的作用
- 使用"格式检查清单"强化要求

### 问题 3：输出冗余内容

**可能原因**：未设置停止词

**解决方案**：
- 在 API 设置中添加 `"stop": ["</options>"]`
- 或在提示词末尾强调："输出到 </options> 后立即停止"

### 问题 4：长对话后掉格式

**可能原因**：上下文过长导致注意力分散

**解决方案**：
- 启用摘要系统
- 定期清理历史消息
- 降低 max_tokens（避免生成过长内容）

---

## 9. 测试清单

在正式使用前，建议测试以下场景：

- [ ] 日常对话场景（3-5 轮）
- [ ] 长对话场景（10+ 轮）
- [ ] 紧张/冲突场景
- [ ] 亲密场景
- [ ] 快速连续对话（测试稳定性）

每个场景检查：
- [ ] `<content>` 标签完整
- [ ] `<options>` 标签完整
- [ ] 四个选项格式正确
- [ ] 无多余输出
- [ ] 无漏标签

---

## 10. 推荐配置总结

### 最佳实践组合

1. **System Prompt**：使用上面的完整模板
2. **API 参数**：`temperature: 0.3`, `stop: ["</options>"]`
3. **模型选择**：DeepSeek-V4-Pro
4. **上下文管理**：启用摘要，保留最近 5-8 条消息
5. **世界书条目**：使用简化版，`order: 100`, `constant: true`

### 预期效果

- 格式稳定性：95%+
- 漏标签概率：<5%
- 冗余输出：基本消除
- 选项质量：稳定

---

文件：`DeepSeek_V4_格式优化指南.md`
