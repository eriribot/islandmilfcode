// 获取指定cg（这里的意思是获取天之音组的第一张图片）
const url = await window.top.BubbleCG.getImage('天之音', 1);
if (url) {
  document.getElementById('myImg').src = url;
}

// 获取随机cg（这里的意思是获取天之音组的随机一张图片）
const url = await window.top.BubbleCG.getRandomImage('天之音');
if (url) {
  document.getElementById('myImg').src = url;
}

// 获取角色主头像（自动回退到全局）
const url = await window.top.BubbleAvatar.getAvatar('望月雫');

// 获取情绪差分头像（获取角色的情绪差分图片）
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '喜悦');
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '愤怒');
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '悲伤');
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '紧张');
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '平和');
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '害羞');
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '嫌弃');
const url = await window.top.BubbleAvatar.getMoodAvatar('望月雫', '爱恋');

// 获取角色主题色
const color = await window.top.BubbleAvatar.getColor('望月雫');
// color = '#58a6ff' 或 null
