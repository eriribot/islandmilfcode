# 快捷回复选项功能

## 功能说明

快捷回复选项功能允许主 API 在生成正文后，自动提供四个符合 `<user>` 人设的行动选项，以千恋万花风格的视觉小说选项形式呈现。

## 前端实现

前端已完成以下实现：

1. **解析函数** (`message-format.ts`): `extractOptionsBlock()` - 从 AI 回复中提取 `<options>` 标签内容
2. **渲染组件** (`render.ts`): `renderOptionsPanel()` - 渲染选项按钮面板
3. **样式** (`styles.css`): `.reader-options` - 千恋万花风格的选项卡片样式
4. **事件处理** (`index.ts`): 点击选项自动填充到输入框并发送

## 提示词注入方法

要启用此功能，需要在角色卡的世界书中添加一个输出格式条目，指示 AI 在每次回复后生成选项。

### 方法一：添加到现有的输出格式条目

如果角色卡已有输出格式说明条目，在其中添加：

```
## 快捷回复选项

在 </content> 标签之后，提供四个符合 <user> 人设的行动选项，用 <options> 标签包裹：

<options>
>选项一：[提供一个谨慎、观察性的行动选项]
>选项二：[提供一个积极、主动以推进任务的行动选项]
>选项三：[提供一个侧重于人际关系或私人互动的行动选项]
>选项四：[提供一个符合当下情景的，带有情色或挑逗意味的NSFW行动选项]
</options>

要求：
- 每个选项应该是 <user> 可能说的话或做的动作
- 选项应该简洁（20-40字），符合当前场景和 <user> 的性格
- 选项一到选项四的风格应该有明显区分
- 选项四可以根据场景氛围调整尺度，不适合时可以是幽默或调侃性质的选项
```

### 方法二：创建独立的世界书条目

创建一个新的世界书条目：

```json
{
  "uid": [自动分配],
  "key": ["输出格式", "快捷回复", "选项"],
  "keysecondary": [],
  "comment": "快捷回复选项格式",
  "content": "在每次 assistant 回复的 <content> 正文之后，生成四个快捷回复选项供 user 选择。\n\n格式：\n<options>\n>选项一：[谨慎观察型选项]\n>选项二：[积极推进型选项]\n>选项三：[人际互动型选项]\n>选项四：[情色/挑逗型选项或幽默选项]\n</options>\n\n每个选项应该：\n- 符合 user 的人设和当前情绪\n- 简洁明了（20-40字）\n- 提供不同的互动方向\n- 选项四根据场景调整尺度",
  "constant": true,
  "selective": false,
  "order": 100,
  "position": "after_char",
  "enabled": true
}
```

## 选项格式示例

### 示例 1：日常对话场景

```
<options>
>选项一：观察她的表情，等待她继续说下去
>选项二：直接询问："你是不是有什么烦心事？"
>选项三：轻轻拍拍她的肩膀，给予安慰
>选项四：凑近她耳边低声说："要不要找个安静的地方聊聊？"
</options>
```

### 示例 2：紧张场景

```
<options>
>选项一：保持沉默，观察周围的情况
>选项二：立即采取行动，冲上前去
>选项三：试图用言语缓和气氛
>选项四：握住她的手，给她一个鼓励的眼神
</options>
```

### 示例 3：亲密场景

```
<options>
>选项一：移开视线，假装什么都没看到
>选项二：主动打破沉默："我们该谈谈了"
>选项三：温柔地抚摸她的脸颊
>选项四：将她拉入怀中，吻住她的唇
</options>
```

## 技术细节

### 解析逻辑

`extractOptionsBlock()` 函数会：
1. 查找 `<options>...</options>` 标签
2. 提取每一行以 `>` 开头的内容
3. 去除 `选项一：`、`选项二：` 等前缀
4. 去除方括号 `[]`（如果存在）
5. 返回纯文本选项数组

### 渲染逻辑

- 选项以 2x2 网格布局显示
- 每个选项是一个可点击的按钮
- 鼠标悬停时有视觉反馈
- 点击后自动填充到输入框并发送

### 样式特点

- 渐变背景（淡紫色调）
- 圆角边框
- 悬停时轻微上浮动画
- 左侧有 `▸` 箭头装饰
- 符合千恋万花的视觉小说美学

## 注意事项

1. **流式生成**：选项仅在 assistant 消息完全生成后显示（`streaming: false`）
2. **标签位置**：`<options>` 应该在 `</content>` 之后，不影响正文提取
3. **选项数量**：建议始终提供 4 个选项，少于 4 个也能正常显示
4. **选项长度**：建议 20-40 字，过长会自动换行
5. **NSFW 选项**：选项四可以根据场景和用户偏好调整尺度

## 禁用方法

如果不需要此功能：
1. 从世界书中移除相关提示词条目
2. 或在提示词中明确说明"不生成 `<options>` 标签"

前端会自动检测，没有选项时不显示选项面板。
