# V07 路线 DDL：未完成的任务

> 记录日期：2026-07-12（Asia/Shanghai）  
> 状态：**候选代码已部分落地，但尚未完成代码审查、类型收口和人工验收。**  
> 本文用于交接，不得据此宣称功能已完成。

## 1. 用户最终确认的需求

### 1.1 路线事实检查器

- 第七章开始后开启。
- 时间下界：`2013-02-08`，即 `SAE_07-1`。
- 时间上界：`2013-03-04`，上界不包含。
- 实际运行范围：`2013-02-08 <= currentDate < 2013-03-04`。
- 进入 `2013-03-04` 后关闭，不再调用路线事实副 API。
- 检查器关闭、失败、漏判或所有事实为 `0`，都不能阻止玩家选择路线。
- 检查器只辅助记录剧情倾向，不拥有最终选择权。

### 1.2 DDL 与三线选择

- 玩家最终只选择三个路线 family：
  - `stay`：留下。
  - `solo`：单飞。
  - `akane`：朱音。
- 旧五个 variant 只能继续作为剧情倾向、内部配置或旧存档兼容信息，不能继续显示成五个玩家最终选择。
- 最终路线不检查 flag eligibility；玩家可以选择三条路线中的任意一条。
- 自由输入和副 API 都不能自动生成 choice receipt。

### 1.3 SAE_07-8 生命周期

- `2013-03-04` 只是 DDL 日期，不能仅凭日期立即弹选择。
- 必须先完成 `SAE_07-8` 正文。
- 正文后的正式 progress 必须成功提交：
  - `mainEvents['SAE_07-8'] = '已结束'` 或正常完成终态。
  - `currentMainEventId = ''`。
- 只有终态进入权威 `statusData` 后，才强制玩家三选一。
- 选择前禁止提交下一段主正文，但玩家仍应能查看刚完成的事件楼层。

### 1.4 游戏开发开关

- `stay` 不自动开启当前游戏开发模块。
- `solo` 开启单飞游戏开发模式。
- `akane` 开启朱音游戏开发模式。
- 每次新路线确认必须生成新的 `confirmationId`。
- 回退后重新选择同一路线，不能复活旧确认实例的游戏开发进度。

### 1.5 代码注释

- 所有关键新逻辑必须写中文注释。
- 注释要解释领域原因、生命周期和失败模式，不能只逐字翻译代码。

## 2. 已修改文件与当前内容

### 2.1 已完成并验证过的前置修改

#### `actions/index.ts`

已经加入 `SAE_07-8` 在 `2013-03-04` 当天合法结束的专用能力：

- 只有正常主正文成功后的 progress 调用持有 `allowSae078SameDayCompletion`。
- 必须同时提交终态和 `当前事件:无`。
- 手机 progress、后台重试和其他旁路没有该权限。
- 保留原有剧情库白名单、日期闸和路由闸。

已执行证据：

- 针对性同日结束断言：`5/5` 通过。
- 旧 V07 运行时合同：`73/73` 通过（这是改动早期执行的结果，后续三线重构后没有再次执行旧合同）。

#### `plot-state-machine/v07.ts`

已经把路线事实检查器窗口改为：

```ts
proposalWindow: {
  start: '2013-02-08',
  end: '2013-03-03',
}
```

已执行边界断言：

- `2013-02-07`：关闭。
- `2013-02-08`：开启。
- `2013-03-03`：开启。
- `2013-03-04`：关闭。

### 2.2 已落代码但尚未完成审查的九个生产文件

#### `plot-state-machine/types.ts`

- 将玩家最终 `PlotRouteId` 改为 `PlotRouteFamilyId`。
- 保留 `PlotRouteVariantId` 作为内部倾向与旧存档兼容。
- 增加三线 advisory 类型。
- choice receipt 兼容 schema v1/v2。
- v2 receipt 增加：
  - `confirmationId`。
  - `anchorFloorIndex`。
- 旧 `variantId`、`basisHash`、`basisFlagIds` 改为兼容可选字段。

#### `plot-state-machine/resolver.ts`

- 五个 variant 继续计算事实完成度。
- 将五变体聚合成 `stay / solo / akane` 三个 advisory family。
- flag 只影响提示，不再影响玩家 choice。
- 有合法 receipt 时直接以 `familyId` 作为权威选择。
- schema v1 receipt 继续按旧 variant 校验。
- schema v2 receipt 校验 `familyId + confirmationId + anchorFloorIndex`。

#### `plot-state-machine/choice.ts`

- 玩家选择目标改为三条 family。
- 删除 flag eligibility 的实际确认阻塞。
- 新增 `isV07RouteChoiceRequired()`。
- DDL 当前只接受 `SAE_07-8` 的 `已结束/已完成`，不把 `跳过/延后` 当正常完成。
- v2 receipt 写入新的 `confirmationId` 和时间线头部 `anchorFloorIndex`。
- 已有 choice 继续锁定；同一路线重复选择为 no-op。

#### `plot-state-machine/memory.ts`

- v2 choice 使用三线与 `confirmationId` 写审计原因。
- 增加 `clearPlotRouteChoiceAfterFloor()`。
- 回退跨过确认楼层时写正式 tombstone，不直接删除 receipt，不恢复完整 memoryDB。
- schema v1 旧 receipt 暂不自动按楼层清除，因为旧数据没有可靠锚点。

#### `plot-state-machine/prompt.ts`

- DDL 后主正文只注入玩家确认的三线 choice。
- choice 写明不得由旧 flag 自动换线。
- DDL 前 flag 背景窗口改为 `2013-02-08～2013-03-03`。
- `SAE_07-8` 正在进行时仍允许注入已有事实背景。

#### `game-development/index.ts`

- `GameDevelopmentState` 升级为 schema v2。
- 增加 `routeConfirmationId`。
- 周提交也记录 `routeConfirmationId`。
- 新 DDL receipt 根据 family 选择内部开发 variant：
  - `akane → akane_core`。
  - `solo → solo_user_exit`，旧集体单飞 receipt 仍兼容原 variant。
- 增加 `isGameDevelopmentRouteChoice()`，只有 `solo / akane` 返回 true。
- 读取时要求状态的 `routeConfirmationId` 与当前 receipt 相同。

#### `phone/render.ts`

- 企划页最终选择从五个 variant 按钮改为三个 family 按钮。
- 旧事实完成度改称“剧情倾向”，明确不阻止选择。
- `stay` 页面提示不会开启单飞/朱音游戏开发。
- 游戏开发页只在 `solo / akane` receipt 下开放。
- 复用现有 CSS，没有修改 `phone/styles.css`。

#### `index.ts`

- 最终确认按 `data-route-family` 读取三线 family。
- 确认时传入 `SAE_07-8` 生命周期状态和时间线头部楼层。
- choice 成功后根据 family 显示是否开启游戏开发。
- 增加主正文提交阻塞包装：DDL 未完成时打开企划页并拒绝下一段正文。
- DDL 第一次被派生为 true 时自动打开一次企划页；玩家可以关闭查看正文，但仍不能继续提交正文。

#### `state/store.ts`

- rollback target 增加源 user 的 reader floor index。
- 回退或删除跨过 v2 choice 锚点时调用 `clearPlotRouteChoiceAfterFloor()`。
- 没有复制完整 memoryDB。

## 3. 已执行验证

### 3.1 构建

已执行：

```text
npm run build:dev
```

结果：webpack 全部编译成功。

注意：构建通过只证明当前生产入口可以打包，不是人工剧情验收。

### 3.2 ESLint

以下八个本轮生产文件单独执行 ESLint，结果通过：

- `plot-state-machine/types.ts`
- `plot-state-machine/choice.ts`
- `plot-state-machine/resolver.ts`
- `plot-state-machine/memory.ts`
- `plot-state-machine/prompt.ts`
- `game-development/index.ts`
- `phone/render.ts`
- `state/store.ts`

`index.ts` 仍有两条既存 lint 错误：

- 文件开头旧 `@ts-ignore`。
- 旧 `require()` 风格导入。

本轮没有顺带修改。

### 3.3 TypeScript 全仓检查

`tsc --noEmit` 没有通过。

仓库已有大量类型债务，包括缺失 `PartialDeep`、`LiteralUnion`、Node 类型、旧 memory/editor/plugin 类型问题等。

本轮重构还让以下未修改区域出现新的兼容型错误：

- `gamedevelop-preview/main.ts` 仍把 resolver choice 当五变体。
- `scripts/simulate-v07-routing.ts` 仍按旧五变体 choice 编写。

用户明确要求本轮不要修改合同脚本，因此这些脚本没有更新。

## 4. 明确未完成的问题

### 4.1 必须人工检查九个生产文件

当前九文件是一次性落下的候选实现，用户尚未逐文件检查。

重点检查：

- 三线 choice 与旧五 variant 是否真正分层。
- 是否还有生产 UI 把 variant 当最终选择。
- `stay` 是否在所有入口都不会打开当前游戏开发模块。
- `solo / akane` 是否只打开各自模式。
- DDL 自动打开与正文提交阻塞是否符合预期。

### 4.2 旧游戏开发状态迁移仍需复核

`game-development/index.ts` 已能把 v1 state 顶层升级到 v2，但旧 `lastSubmission` 内没有 `routeConfirmationId`。

当前强类型把新字段视为必填，因此建议下一步明确处理：

```text
旧 lastSubmission 存在
→ 迁移时补 routeConfirmationId
```

否则运行时虽然大概率不立刻崩溃，但旧提交对象不满足新 schema 的完整合同。

### 4.3 `promptWindow` 仍是旧模型字段

`plot-state-machine/v07.ts` 仍保留：

```ts
promptWindow: {
  start: '2013-03-04',
  end: '2013-03-31',
}
```

新版 `choice.ts` 已不使用它阻止选择，phone 也改用 SAE 生命周期。

但这个字段名和旧日期仍容易误导后续维护者。需要决定：

- 删除/重命名为 legacy 字段；或
- 明确只保留旧兼容，不再作为 DDL 权威。

### 4.4 旧错误码仍保留

以下错误码为兼容暂时保留，但新版正常路径不应再产生：

- `outside_choice_window`
- `route_not_eligible`

需要人工决定是否在完成旧调用方迁移后删除。

### 4.5 旧预览和合同脚本未迁移

未修改：

- `gamedevelop-preview/main.ts`
- `scripts/simulate-v07-routing.ts`
- `scripts/verify-v07-post-turn-routing.ts`

这些文件仍描述旧五变体选择和旧日期窗。用户本轮明确表示由自己检查，不要求更新合同脚本。

### 4.6 phone CSS 未做真实页面检查

三线 UI 复用了旧 family/variant 样式，但没有在 `8000` 页面进行真实手机尺寸检查。

需要人工确认：

- 三张路线卡是否大小正常。
- “剧情倾向”文案是否清晰。
- DDL 自动打开后是否遮挡或无法关闭。
- 关闭企划页后是否能查看 `SAE_07-8` 正文。
- 再次点击发送是否确实重新引导到企划页。

### 4.7 回退仅对 v2 receipt 有可靠保证

- v2 receipt 有 `anchorFloorIndex`，可以按楼层边界清除。
- v1 receipt 没有稳定锚点，目前保留，不自动猜测清除。
- `UiMessage.id` 仍然不能作为跨重载锚点。
- 自动剧情 flag 的 supersede 前驱恢复问题没有在本轮处理；由于 flag 已不再阻止三线 choice，它不会卡死 DDL，但仍是记忆库回退债务。

### 4.8 没有真实 SAE_07-8 正文验收

尚未在真实酒馆页面执行：

```text
SAE_07-8 进行中
→ 正文满足结束条件
→ progress 同日写已结束 + 当前事件无
→ 状态持久化
→ 自动打开三线 DDL
→ 未选择时阻止下一段正文
```

这是最重要的人工验收链。

## 5. 推荐续接顺序

1. 用户先逐文件检查本轮九个生产文件。
2. 先修 `game-development/index.ts` 的旧 `lastSubmission` 迁移完整性。
3. 决定是否保留 `promptWindow` 和两个旧错误码。
4. 用 UTF-8 `rg` 再查一次生产代码是否残留五变体最终选择入口。
5. 在 `8000` 页面人工跑真实 `SAE_07-8` 生命周期。
6. 检查三线 DDL、二次确认和刷新持久化。
7. 检查 `stay / solo / akane` 三种下游行为。
8. 检查确认楼层前后两侧的回退。
9. 检查同一路线重新选择时旧游戏开发状态不会复活。
10. 若用户之后授权，再迁移旧 preview/合同脚本。

## 6. 不得误报

当前只能表述为：

> 三线 DDL 候选代码已进入生产文件，开发构建成功，等待用户代码检查和真实页面验收。

当前不能表述为：

- 已完成。
- 已正式可用。
- 已通过 SAE_07-8 正文验收。
- 已证明回退所有边界正确。
- 已证明旧存档迁移正确。
